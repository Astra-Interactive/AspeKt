main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ DiscordSRV, LuckPerms, Vault ]
depend: [ ]
loadbefore: [ Vault ]
#libraries: [ "${libraries}" ]
commands:
  sit:
  tellchat:
  rtp:
  atemframe:
  maxonline:
  entities:
  aesreload:
  adminprivate:
  menu: