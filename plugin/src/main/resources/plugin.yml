main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ DiscordSRV, LuckPerms, Vault, Esssentials, Towny ]
depend: [ ]
loadbefore: [ ]
#libraries: [ "${libraries}" ]
commands:
  rtpbypass:
  sit:
  tellchat:
  rtp:
  atemframe:
  maxonline:
  entities:
  aesreload:
  adminprivate:
  menu:
  invclose: